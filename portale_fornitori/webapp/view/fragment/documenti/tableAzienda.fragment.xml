<c:FragmentDefinition
	xmlns="sap.m"
	xmlns:c="sap.ui.core"
>
<Table
      sticky="HeaderToolbar,ColumnHeaders"
      growing="true"
      autoPopinMode="true"
      class="table"
      items="{docModel>/}"
      > 

      <columns>
        <Column width="100px"  hAlign="Left">
          <Text text="{i18n>Documento}" />
        </Column>
        <Column width="60px"  hAlign="Center">
          <Text text="{i18n>Scarica}" />
        </Column>
        <Column width="100px"  hAlign="Center" visible="{= ${userModel>/tipoUtente} === 'Interno' ? true : false }">
          <Text text="{i18n>Validazione}" />
        </Column>
        <Column width="60px"  hAlign="Center" visible="{= ${userModel>/tipoUtente} === 'Interno' ? false : true }">
          <Text text="{i18n>Allega}" />
        </Column>
        <Column width="60px"  hAlign="Center">
          <Text text="{i18n>Stato}" />
        </Column>
        <Column width="130px" hAlign="Center" visible="{= ${userModel>/tipoUtente} === 'Interno' ? true : false }">
          <Text text="{i18n>Datacaricamento}" />
          </Column>   
          <Column width="60px"  hAlign="Center" visible="{= ${userModel>/tipoUtente} === 'Interno' ? false : true }">
            <Text text="{i18n>Esito}" />
          </Column>
        <Column width="90px" hAlign="Center">
        <Text text="{i18n>Scadenza}" />
      </Column>          
      </columns>
      <items>
        <ColumnListItem
         
        >
          <cells>
            <Text text="{docModel>descrizione}" />
          </cells>
          <cells>
            <Button
                visible="{= !${docModel>scarica}? false : true}"
                icon="{docModel>scarica}"
                type="Emphasized"
            />
          </cells>
          <cells>          
            <Select width="150px" items="{docModel>validazione}">
              <c:Item key="{docModel>value}" text="{docModel>text}"/>
            </Select>
          </cells>
          <cells>
            <Button
                type="Emphasized"
                icon="{docModel>allega}"   
                press="onOpenMaskAllega"             
            />
          </cells>
          <cells>
            <c:Icon
                alt="Download"              
                size="1.3rem"
                src="{docModel>stato}"
                color="{docModel>color}"
            />
          </cells>
          <cells>
            <Text text="{docModel>data}" />
          </cells>
          <cells>
            <Text text="{docModel>esito_validazione}" />
          </cells>
          <cells >
            <DatePicker textAlign="Center" displayFormat="dd/MM/yyyy" value="{docModel>scadenza}" editable="false" />
          </cells>
          
        </ColumnListItem>
      </items>
      </Table> 
</c:FragmentDefinition>